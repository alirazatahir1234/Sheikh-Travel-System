/**
 * Fleet dashboard mock fixtures for the Admin Portal dashboard selector.
 *
 * Re-exported from the canonical fleet-management source so both the `/dashboard`
 * (selector) and `/fleet/dashboard` (hub) routes share a single set of fixtures.
 */
export {
  FLEET_PRIMARY,
  FLEET_SECONDARY,
  FLEET_NEUTRAL,
  UTILIZATION_MOCK,
  FUEL_MAINTENANCE_MOCK,
  QUICK_ACTIONS_MOCK,
  ALERTS_MOCK,
  ACTIVITIES_MOCK,
  ASSIGNMENTS_MOCK
} from '../../fleet-management/fleet-dashboard/fleet-dashboard.mock';
