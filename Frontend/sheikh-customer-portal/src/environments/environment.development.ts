export const environment = {
  production: false,
  /** Dev server uses `proxy.conf.json` to forward `/api` to the API (default http://127.0.0.1:5082). */
  apiRoot: '/api',
  googleMapsApiKey: ''
};
