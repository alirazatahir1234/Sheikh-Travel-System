# OS
.DS_Store
Thumbs.db

# Cursor agent / debug session logs (never commit)
.cursor/*.log
.cursor/debug-*.log
.cursor/debug-*.ndjson

# Android / Play signing material (never commit)
*.jks
*.keystore
**/key.properties
**/service-account*.json
